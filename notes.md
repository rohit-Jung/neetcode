## approaches

- when it comes to duplicate, you can use `set`
- for `count` related problem you can have a `hashmap`
- a unique way i found is anagram was to utilize the `charCode/ASCII code` and maintain a count of it to compare two strings

- for `twoSum` like problem be careful what's being stored and what's being retrieved. also do you want to check the current thing on map ? 
